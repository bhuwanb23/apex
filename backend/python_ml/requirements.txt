# AQX ML Microservice — Python dependencies
# Install into the project venv only:  python -m venv .venv && .venv/Scripts/pip install -r requirements.txt

# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0

# Data science / ML
numpy>=1.26
pandas>=2.2
scipy>=1.13
scikit-learn>=1.4
lifelines>=0.29

# NFL data (Step 10) — latest release is 0.x; newer major versions don't exist
nfl-data-py>=0.3.3

# Testing
pytest>=8.2
httpx>=0.27
