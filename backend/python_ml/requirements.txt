# Apex ML Microservice — Python dependencies
# Install into the project venv only:  python -m venv .venv && .venv/Scripts/pip install -r requirements.txt

# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0

# Data science / ML
numpy>=1.26
pandas>=2.2
scipy>=1.13
scikit-learn>=1.4
lifelines>=0.29

# Optional: OpenAI story enhancement (Step 9). The service falls back to
# templates when this is absent, so it is only needed for the richer path.
# openai>=1.0

# NFL data (Step 10) — nfl-data-py 0.3.3 pins pandas<2.0, incompatible with
# Python 3.13 / modern pandas; installed separately in Step 10 if viable.
# nfl-data-py>=0.3.3

# Testing
pytest>=8.2
httpx>=0.27
